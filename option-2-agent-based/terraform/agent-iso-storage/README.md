# Deprecated

Agent ISO bucket, upload, and PAR are now in [`terraform-stacks/create-cluster/agent_iso.tf`](../../terraform-stacks/create-cluster/agent_iso.tf).

Use the single-state Option 2 flow in [../README.md](../README.md).
